#!/usr/bin/env bash
#
# wplocal
#
# Shortcut for running WP-CLI commands through LocalWP's environment.
#
# Usage:
#   wplocal plugin list
#   wplocal search-replace 'old.test' 'new.test'

exec silentlocalwpshell wp "$@"
