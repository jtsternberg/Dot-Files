#!/usr/bin/env bash
# Deprecated alias — the triage launcher is now `watchdog-chat`. Forwards to it;
# remove this shim once muscle memory catches up.
echo "note: 'battery-watchdog-assess' is now 'watchdog-chat' — forwarding." >&2
exec "$(dirname "$0")/watchdog-chat" "$@"
